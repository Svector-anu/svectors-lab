---
type: Issue
id: ISS-001
title: Fleet zero-token harness failure (grok-4.5)
status: open
severity: critical
category: config
detected_by: skill-health
detected_at: 2026-07-19T18:33:57Z
affected_skills: [digest, fear-divergence, hunter-22, skill-health, vuln-scanner]
root_cause: zero-token harness failure (input_tokens=0, output_tokens=0, total_cost_usd=0; also surfaces as iterations=[] / modelUsage={})
fix_pr: null
---

## Update 2026-08-25 (01:07 audit) — critical set rotates again

The same intermittent zero-token harness signature remains systemic across the fleet. Two enabled skills are currently CRITICAL and have been added to this existing issue rather than receiving duplicates:

- `skill-health` — 5 consecutive failures; last success 2026-08-22T18:22:20Z; zero input/output tokens.
- `fear-divergence` — 3 consecutive failures; last success 2026-08-23T09:45:38Z; zero input/output tokens.

The previously affected `digest`, `hunter-22`, and `vuln-scanner` remain attached because lifetime success rates keep them DEGRADED under the health contract even though each has recorded a later successful run. A second systemic signature is now visible in `heartbeat` and `github-trending`: `managed:kimi-code` requires login. That pair is DEGRADED, not CRITICAL/FLAPPING, so this audit reports it but does not file an issue.

Current `affected_skills`: `[digest, fear-divergence, hunter-22, skill-health, vuln-scanner]`.

## Update 2026-08-22 (18:19 audit) — churn continues: 2 recover, 2 new entrants

The zero-token harness failure keeps rotating across the fleet — same intermittent signature (`total_cost_usd:0`, `input_tokens:0`/`output_tokens:0`), a different subset failing each audit.

**Recovered** since the 08-21 18:00 audit (cf=0, fresh successful run, quality 4) — removed from `affected_skills`:
- `defi-overview` — last_status=success, cf=0, last success 2026-08-22T13:08:25Z (3 clean runs today).
- `heartbeat` — last_status=success, cf=0, last success 2026-08-22T08:17:03Z.

**Added** to `affected_skills` (crossed CRITICAL with the identical zero-token signature — grouped here, not filed separately, per the same-root-cause contract):
- `hunter-22` — cf=4, last success 2026-08-21T11:46:02Z (~1d down), `total_cost_usd:0`.
- `vuln-scanner` — cf=3, last success 2026-08-22T06:08:50Z (clean scan this morning, then 3 zero-token fails; last_failed 2026-08-22T17:56:04Z). Was WARNING (cf=2) at the prior audit; the recent `chore(cron): vuln-scanner failed` commit is the same signature.

**Still failing:** `digest` — cf=3, last success 2026-08-21T21:49:15Z, `total_cost_usd:0` (unchanged from prior audit).

Current `affected_skills`: `[digest, hunter-22, vuln-scanner]` — issue stays **open**. The intermittent, whole-fleet, rotating nature (skills succeed then re-fail within a day) points at a harness/auth/rate transient, not per-skill logic — consistent with the original grok-4.5 zero-token root cause. Closing when the whole fleet holds cf=0 across a full cycle is skill-repair's job.

## Update 2026-08-21 (18:00 audit) — recovery continues, one new entrant

The zero-token outage keeps resolving from the top.

**Recovered** since the 13:57 audit (cf=0, fresh successful run) — removed from `affected_skills`:
- `narrative-tracker` — last_status=success, cf=0, last success 2026-08-21T13:59:00Z, quality 4.
- `skill-health` — last_status=success, cf=0, last success 2026-08-21T13:58:57Z (the recovery point flagged last run confirmed).

**Added** to `affected_skills`: `digest` — crossed CRITICAL (cf=3, last success 2026-07-28, 24d down) with the identical zero-token signature (`total_cost_usd:0`, empty iterations). Grouped here to avoid a duplicate same-root-cause issue, per the health contract.

**Still failing** on the zero-token signature: `defi-overview` (cf=7, 24d down) and `heartbeat` (cf=4, 22d down).

Current `affected_skills`: `[defi-overview, heartbeat, digest]` — issue stays **open**. `github-trending` (ISS-002) fully recovered this run and that issue is now resolved.

## Update 2026-08-21 — partial recovery

The fleet outage is partially resolving.

**Recovered** (consecutive_failures=0, fresh successful run, quality ≥3) — removed from `affected_skills`:
- `token-pick` — last success 2026-08-21, quality 5
- `fear-divergence` — last success 2026-08-21, quality 3
- `x402-monitor` — consecutive_failures=0, last_status=success (weekly Tue; stale but not failing)
- `picks-tracker` — consecutive_failures=0, last_status=success (weekly Sun)

**Still failing** on the same zero-token signature: `narrative-tracker` (cf=6), `defi-overview` (cf=6), `skill-health` (cf=138 — this very run is the likely recovery point).

**Added** to affected_skills: `heartbeat` — crossed CRITICAL (cf=3, last success 2026-07-30) with the empty-iterations variant of the same signature (`iterations:[]`, `modelUsage:{}`). Grouped here rather than filed separately to avoid a duplicate same-root-cause issue.

Not the same root cause (filed separately): `github-trending` → see **ISS-002** (read-only workspace write-lock, not zero-token).

## What happened

All seven scheduled skills with run history are failing with the same signature: the harness records `total_cost_usd:0` and zero input/output tokens — the model never starts. Last real successes cluster around 2026-07-08 to 2026-07-11. Heartbeat recovered once today (`last_status=success`, consecutive_failures reset) but fleet success rates remain ~1–3%.

## Signal

- consecutive_failures: 119–150 per skill
- days_since_last_success: 8–11 (x402-monitor longest at 11d)
- last_error: "total_cost_usd:0 … input_tokens:0 … output_tokens:0"
- related skills: fear-divergence, picks-tracker, narrative-tracker, x402-monitor, token-pick, skill-health, defi-overview
- model/harness: grok-4.5 / grok (from today's heartbeat ambient)
- suggested_action: FIX CONFIG — verify GROK_CREDENTIALS / XAI_API_KEY and harness routing; zero tokens means auth or model path, not skill logic
