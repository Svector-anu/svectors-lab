Forced target (var=fireblocks/fireblocks-sdk-js). TypeScript client SDK for Fireblocks custody API, 84 stars, PVR enabled, 13 source files. semgrep=ok 9 raw hits, all GitHub Actions mutable-action-tag warnings in .github/workflows/ (CI hardening only, no runtime code hit). trufflehog=ok 0 verified secrets (filesystem + full git history). osv=ok 28 dependency CVEs across 12 packages; prior-art check found open issue #333 (base-x) and open stale PR #314 (qs, target 6.13.1 no longer covers 4 newer CVEs). Ran `npm audit fix --package-lock-only` (no --force, package.json untouched) -> fixed 9 packages (qs 6.11.1->6.16.0, lodash 4.17.21->4.18.1, form-data 4.0.5->4.0.6 CVE-2026-12143, base-x 4.0.0->4.0.1 closes #333, jws 3.2.2->3.2.3, plus 4 dev-only transitive bumps) verified via before/after osv-scanner diff (28->4). Filed PR #344 (https://github.com/fireblocks/fireblocks-sdk-js/pull/344), commented on #314 pointing to it, commented on #333 linking the fix. 3 dependency findings left unfixed and noted in the PR body: @stablelib/ed25519 GHSA-x3ff-w252-2g7j (signature malleability, no patched release exists even at latest 2.0.2), uuid GHSA-w5hq-g745-h8pq (needs major bump), decode-uri-component GHSA-vcc3-ghjq-m6fr (needs query-string major bump). fuzz=skip (no fuzz/fuzz_targets). agentic=ok: reviewed all 13 source files incl. api-token-provider.ts (JWT signing only, jwt.verify never called so the jws HMAC-verification CVE is not reachable in this repo's own call path even before the fix) and api-client.ts/fireblocks-sdk.ts -- 1 candidate surfaced: nearly every SDK method interpolates a caller-supplied ID (vaultAccountId/assetId/connectionId/etc.) directly into the request path with no encodeURIComponent or format validation; local axios test confirmed literal dot-dot-slash segments survive unmodified into the outbound request config.url. Could not clear A4.5 verification because the second half of the claim (whether Fireblocks' own production API/edge normalizes or rejects a traversal path) depends on infrastructure outside this repo and cannot be safely tested against a live third-party service. Left as needs-verification, not filed, surfaced to operator -- if a caller forwards unsanitized external input as an ID parameter, this could redirect an authenticated request to an unintended Fireblocks endpoint.