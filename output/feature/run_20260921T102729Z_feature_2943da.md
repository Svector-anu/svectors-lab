external-feature: Svector-anu/svectors-lab — added MiniAeon manual workflow Operator var passthrough.

PR: https://github.com/Svector-anu/svectors-lab/pull/96  
Head: `ai/miniaeon-workflow-var-input` @ `447ef8c5268af42f04dca4a4b890522f180c779a`

What changed:
- `.github/workflows/miniaeon.yml`: added `workflow_dispatch.inputs.var`.
- `.github/workflows/miniaeon.yml`: passes `INPUT_VAR` through as `miniaeon run <skill> --var <value>` when a manual skill run includes a var.
- `scripts/tests/test_miniaeon_workflow.sh`: executes the workflow’s real `Run skill` shell body with a fake MiniAeon binary and verifies:
  - `workflow_dispatch + skill + var` → `run feature --var external:Svector-anu/svectors-lab`
  - `workflow_dispatch + skill + no var` → `run feature`
  - `schedule` with no skill → `run-due`
  - `repository_dispatch` ignores direct skill/var and remains wake-only → `run-due`
- `.github/workflows/ci-tests.yml`: runs the new regression test when the MiniAeon workflow changes.

Verification:
- `bash scripts/tests/test_miniaeon_workflow.sh` → passed.
- `bash scripts/tests/test_workflow_harness_choices.sh` → blocked locally by missing Python module `yaml`; CI installs `pyyaml==6.0.2` before running this test.

Logged:
- appended `### feature` entry to `memory/logs/2026-09-21.md`.

## Summary
- Built targeted manual MiniAeon workflow dispatch vars for `Svector-anu/svectors-lab`.
- Created PR #96.
- Modified files in PR: `.github/workflows/miniaeon.yml`, `.github/workflows/ci-tests.yml`, `scripts/tests/test_miniaeon_workflow.sh`.
- Follow-up: let CI run the PyYAML-backed existing workflow test in its normal environment.
