## Vuln Scanner — 2026-08-26

### Summary

This run attempted to execute the vuln-scanner **scan** arm with an auto-selected trending GitHub repository target, but found **no eligible repositories with a safe private disclosure channel** (no `SECURITY.md` and private vulnerability reporting disabled) in the sampled set. To avoid creating undisclosable zero-days, the run did not perform any code-level vulnerability analysis and did not generate or publish any findings.

### Target selection

- **Search window**: public repositories created after 2026-08-12, sorted by stars (GitHub Search API).
- **Sample size**: top 5 repositories by stars.
- **Language filter**: all sampled repos were in languages the scanner can reason about (TypeScript, JavaScript, Python).
- **Safety filter**: each candidate was checked for:
  - Presence of a `SECURITY.md` contact file.
  - Private vulnerability reporting status via `GET /repos/{owner}/{repo}/private-vulnerability-reporting`.

All sampled repositories had **private vulnerability reporting disabled** and returned **404 for `SECURITY.md`**, so none met the minimum safe-channel requirement for code vulnerability disclosure.

### Scanner sources

Because no eligible target was selected, no scanners were run:

- **semgrep**: skipped (no eligible repo)
- **trufflehog**: skipped (no eligible repo)
- **osv-scanner**: skipped (no eligible repo)
- **fuzz / cargo-fuzz**: skipped (no eligible repo)
- **agentic logic audit**: skipped (no eligible repo)

### Findings

- **Candidates reviewed**: 0 (no code or dependency findings were generated).
- **Confirmed findings**: 0.
- **Channels used**: none (no reports filed, no drafts staged).

### Deduplication / future runs

- No entries were added to `memory/vuln-scanned.json` because no repository was scanned.
- Future vuln-scanner runs can safely re-attempt target selection on a larger or different trending set; this run did not touch any repo’s code or dependencies.

### Operational notes

- The **do-no-harm** policy was enforced: in the absence of a safe private disclosure channel, the scan arm aborted before any code analysis, avoiding the creation of undisclosable findings.
- No `./notify` call was made for this run; the main artifact is this report and the log entry in `memory/logs/2026-08-26.md`.

