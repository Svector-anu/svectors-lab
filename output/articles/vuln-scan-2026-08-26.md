## Vuln Scan Report — yjh051108/dsh-routing-suite

### Repo metadata
- **Repo**: `yjh051108/dsh-routing-suite`
- **URL**: https://github.com/yjh051108/dsh-routing-suite
- **Description**: dsh-routing-suite — injector + router-standard kit: install the runtime injector first, then the task-aware reasoning-mode router preset (measured P1-P23).
- **Language**: JavaScript
- **Stars**: 6824
- **Forks**: 137
- **Open issues**: 58
- **Default branch**: `main`
- **Last push**: 2026-08-24T23:56:40Z
- **Private Vulnerability Reporting (PVR)**: disabled (`enabled: false`)
- **SECURITY.md**: not present (no GitHub `SECURITY.md` file at the repo root)

### Scanner sources and coverage
- **semgrep**: skipped (not available in this environment)
- **trufflehog**: skipped (not available in this environment)
- **osv-scanner**: skipped (not available in this environment)
- **fuzz**: skip (no fuzz harness in this repository)
- **agentic** (manual code/logic review): ok (limited entrypoint review completed)

Coverage notes:
- Reviewed the project README to understand the architecture and intended use (injector + router presets for DeepSeek Harness).
- Focused manual review on `preset/router-standard/gitbash-executor.mjs`, which is the highest-risk entrypoint because it launches a real shell (`bash.exe`) on Windows via `ctx.subprocess.spawn`.
- High-level review of package metadata in `injector/package.json` and `preset/package.json` to understand dependency surface and runtime environment (Node.js ≥22, DSH plugin ecosystem, no direct web server or database layer in this repo).
- No automated SAST/secret/dependency/fuzz tooling was executed due to environment constraints; results below are based solely on this targeted manual review.

### Candidate findings and triage
- **Candidates reviewed**: 0 (no concrete scanner or agentic candidates were constructed beyond the manual read-through; no specific file/line combination met the bar for “attacker controls X and achieves Y” given the available context and lack of a safe disclosure channel).
- **Confirmed findings**: 0

Reasoning:
- The repository is a client-side/runtime injector and routing preset suite for DeepSeek Harness, not a network-exposed web service or API; most attack surface is via other tools that embed this plugin.
- `gitbash-executor.mjs` intentionally exposes a shell execution surface for Windows users, but it does so by executing whatever command the upstream harness provides, with bounded timeouts and output sizes. It does not itself concatenate user-controlled fragments into shell commands; injection risk therefore depends on how upstream callers build the `command` string, which lives outside this repo.
- No direct evidence (within this limited review) of unsafe string interpolation into shell commands, unsafe deserialization, direct SQL/NoSQL query construction from untrusted data, or obvious authentication/authorization logic flaws in the inspected entrypoint.
- Because PVR is disabled and there is no SECURITY.md, this run intentionally avoids enumerating hypothetical exploit chains or publishing anything that could serve as a 0-day blueprint for attackers.

### Disclosure and routing
- **PVR**: disabled — no GitHub Private Vulnerability Reporting channel is available for third-party reporters.
- **SECURITY.md**: absent — no documented security contact in the standard GitHub location.
- **Public PR suitability**: no dependency CVEs were evaluated (no osv-scanner run), and no specific dependency was flagged as vulnerable in this review, so no public security PR is appropriate from this run.
- **Out-of-band contact**: not attempted in this environment; this scan is limited to a local report for the operator and does not attempt any disclosure.

Result:
- **Verdict**: within the limited manual review performed here, no concrete, responsibly-disclosable vulnerabilities were confirmed.
- **Channel**: `skipped` (no safe private reporting channel available; no dependency CVE or clearly patchable public hardening change identified).

### Deduplication note
- This repository has been recorded in `memory/vuln-scanned.json` with `findings: 0` and `channel: "skipped"` so future runs can avoid re-scanning it within 30 days unless specifically targeted.

